# ATS GitHub Pages Recovery

This repository is deployed as a static site through GitHub Actions.

Required GitHub setting:

1. Open **Settings → Pages**.
2. Set **Build and deployment → Source** to **GitHub Actions**.
3. Push or upload this project to the `main` branch.
4. Open **Actions → Deploy ATS to GitHub Pages** and confirm both jobs pass.

The legacy branch-based Pages builder is no longer used.
